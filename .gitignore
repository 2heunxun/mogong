### IDE ###
.idea/
*.iml
*.ipr
*.iws
.vscode/

### OS ###
.DS_Store
Thumbs.db

# backend/, frontend/ 각각에 자체 .gitignore가 있습니다
# (build 산출물, node_modules, 로컬 DB 파일, .env 등)

### 루트에서 잘못 실행했을 때 생기는 로컬 DB 파일 방지 ###
/data/

### 문서 / 예시 env 파일 (로컬에만 유지, 저장소에는 커밋하지 않음) ###
*.md
.env.example
